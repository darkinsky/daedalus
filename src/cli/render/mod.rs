// Intentionally left empty.
//
// This directory previously held a split-out version of `cli::render`,
// but the module was kept as a single file (`cli/render.rs`) to avoid
// filesystem layout churn. The file remains so the directory is not
// orphaned; it is not loaded by the module tree.